# Document ledger CI gates

The review requires:

- browser document HCV1, batch, gateway and protocol contract tests;
- agent-gateway document service, HTTP, signing and PostgreSQL adapter tests;
- application of all PostgreSQL migrations; and
- the existing full Hestia verification workflow.
