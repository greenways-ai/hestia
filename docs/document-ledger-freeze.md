# Document ledger review scope

This change is frozen for integration review. Further document features should
follow only after the signed batch, Hara OT, PostgreSQL prepare/sign/commit and
conflict receipt path passes repository CI and migration application checks.
